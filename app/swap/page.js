// Scaffold — implemented after the smart contract
// (solana-contracts/programs/craft_fund).
export const dynamic = 'force-dynamic';

export default function SwapPage() {
  return (
    <main>
      <div className="panel">
        <div className="panel-label">USDC ↔ SOL Swap</div>
        <p style={{ color: 'var(--text-muted)', fontFamily: 'var(--font-mono)', fontSize: 13 }}>
          Scaffold — functionality will be implemented after the smart contract
          (solana-contracts/programs/craft_fund). Currently shows the page
          structure only.
        </p>
      </div>
    </main>
  );
}
